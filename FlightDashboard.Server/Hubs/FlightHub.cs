using Microsoft.AspNetCore.SignalR;

namespace FlightDashboard.Server.Hubs
{
    public class FlightHub : Hub
    {
        // You can add methods here if you want to allow clients to call server-side logic.
        // For now, this can be left empty for server-to-client notifications.
    }
}